#include <stdio.h>

/**
 * @description ? 
 * @performance 메모리: ? KB, 동작시간: ? ms
 * @author java08
 */

int main(void) {
    int a, b;
    scanf("%d%d",&a, &b);
    printf("%d\n%d\n%d\n%d\n%d", a+b, a-b,a*b,a/b,a%b);
    return 0;
}
